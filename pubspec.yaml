name: clipboard_all_types
description: This package get all types of data from clipboard
version: 0.0.1
homepage: https://github.com/wirdes/clipboard_all_types

environment:
  sdk: ">=2.18.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
